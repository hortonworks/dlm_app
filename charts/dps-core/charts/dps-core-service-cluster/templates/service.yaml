apiVersion: v1
kind: Service
metadata:
  name: {{ template "dps-core-service-cluster.fullname" . }}
  labels:
    app: {{ template "dps-core-service-cluster.name" . }}
    chart: {{ template "dps-core-service-cluster.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "dps-core-service-cluster.name" . }}
    release: {{ .Release.Name }}
