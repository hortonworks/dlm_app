apiVersion: v1
kind: Service
metadata:
  name: {{ template "dps-core.fullname" . }}
  labels:
    app: {{ template "dps-core.name" . }}
    chart: {{ template "dps-core.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "dps-core.name" . }}
    release: {{ .Release.Name }}
