# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# cluster

GET     /api/clusters                                  controllers.Clusters.list

GET     /api/clusters/pairs                            controllers.Clusters.listPairs()